# Observation log — <effort name>, session <n>

Date: <YYYY-MM-DD>
Participant: <name, role, and how they relate to the job being studied>
Facilitator / note-taker: <names>
Prototype build: <path or link, and its revision>
Tasks given: <the exact words used, verbatim>

---

## Tags

| Tag | Meaning |
|---|---|
| `RULE` | A constraint the participant asserted |
| `EX` | A concrete run — real data in, real outcome out |
| `TERM` | Their word for a thing, and the word we had been using |
| `FRIC` | Where they stalled, backtracked, or misread |
| `ASSUM` | Something the prototype bakes in with no evidence |
| `Q` | An open question |
| `CHOICE` | Direction taken, direction rejected, and why |

One line per observation. Do not tidy during the session. Do not interpret — record.

Live, capture only `FRIC` and `EX`; a recording cannot reconstruct those. Fill in the other five
from the transcript afterwards, and have someone who was in the room correct the pass.

---

## Log

```
RULE
EX
TERM
FRIC
Q
ASSUM
CHOICE
```

---

## Immediately after the session (5 minutes, before the next one)

- What surprised us:
- What we would change in the prototype before the next participant (RITE):
- Does this session move the riskiest assumption toward confirmed or refuted?
